<h1>User List</h1>
<ul>
  <% @users.each do |user| %>
    <li>
    <%= image_tag user.photo, alt: user.name, width: '100' %>
      <h2><%= user.name %></h2>
      <p><%= user.bio %></p>
      <p>Number of Posts: <%= user.posts_counter %></p>
    </li>
  <% end %>
</ul>